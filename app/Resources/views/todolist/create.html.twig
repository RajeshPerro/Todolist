{% extends 'base.html.twig' %}

{% block body %}
   <div class="row" style="margin: 30px; margin-bottom: 10px !important;">
       <div class="col-md-12 text-center">
           <h2 class="jumbotron">{{ message }}</h2>
       </div>

   </div>
    <div class="row" style="margin-left: 100px; margin-bottom: 30px !important;">
        <div class="col-md-8">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
